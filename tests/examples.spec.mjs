import {test,expect} from '@playwright/test';
test('range sample exposes coarse detail and resumes from its buttons',async({page})=>{const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('/examples/range.html');await page.click('#base');await expect(page.locator('#status')).toContainText('Partial detail');await page.click('#tile');await expect(page.locator('#full')).toBeEnabled();await expect(page.locator('#status')).toContainText('Partial detail');await page.click('#full');await expect(page.locator('#status')).toContainText('Full detail');expect(errors).toEqual([]);});
